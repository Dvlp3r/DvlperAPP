<p id="notice"><%= notice %></p>

<h1>User Stories</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>App</th>
      <th>Persona</th>
      <th>Attribute</th>
      <th>Goal</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_stories.each do |user_story| %>
      <tr>
        <td><%= user_story.title %></td>
        <td><%= user_story.app %></td>
        <td><%= user_story.persona %></td>
        <td><%= user_story.attribute %></td>
        <td><%= user_story.goal %></td>
        <td><%= link_to 'Show', user_story %></td>
        <td><%= link_to 'Edit', edit_user_story_path(user_story) %></td>
        <td><%= link_to 'Destroy', user_story, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User Story', new_user_story_path %>
