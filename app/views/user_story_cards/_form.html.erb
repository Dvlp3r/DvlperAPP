<%= form_for(user_story_card) do |f| %>
  <% if user_story_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_story_card.errors.count, "error") %> prohibited this user_story_card from being saved:</h2>

      <ul>
      <% user_story_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :app %>
    <%= f.text_field :app %>
  </div>

  <div class="field">
    <%= f.label :maintitle %>
    <%= f.text_field :maintitle %>
  </div>

  <div class="field">
    <%= f.label :persona %>
    <%= f.text_field :persona %>
  </div>

  <div class="field">
    <%= f.label :trait %>
    <%= f.text_field :trait %>
  </div>

  <div class="field">
    <%= f.label :goal %>
    <%= f.text_field :goal %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
